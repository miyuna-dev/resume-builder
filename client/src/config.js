const port = "http://localhost:8000";

const API_PUBLIC_URL = `${port}`;
const API_BASE_URL = `${port}/api`;
const API_AUTH_URL = `${port}/api/auth/`;
const API_CREATE_URL = `${port}/api/resume/`;

export {
    API_PUBLIC_URL,
    API_BASE_URL,
    API_AUTH_URL,
    API_CREATE_URL,
}